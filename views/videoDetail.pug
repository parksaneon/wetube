extends layouts/main

block content
    .video_player
        video(src=`/${video.fileUrl}`)
    .video_info
        a(href=routes.editVideo(video.id)) Edit video
        h5.video_title=video.title
        span.video_views=video.vides
        p.video_description=video.description  